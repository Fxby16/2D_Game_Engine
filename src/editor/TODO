- file browser
- project serializer
- images preview
- tilemap editor
- project export
- camera

- if the same texture is required more than once, create only one and pass shared_ptr in scene deserialization